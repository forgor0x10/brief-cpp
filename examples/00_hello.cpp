// Necessary headers
// You CAN include <brief/brief.hpp> as a prelude,
// but it's recommended to include only what you need
#include <brief/aliases.hpp>
#include <brief/io.hpp>

// Entry point
// The return type is deducted automatically
fn main() {
    // Say the thing!!
    println("Hello world!");

    // `return 0;` can be safely omitted since C++11
}
